# Required metadata
sonar.projectKey=Faryal-Chess-Game
sonar.projectName=Chess Game
sonar.projectVersion=1.0

# Path to source files
sonar.sources=src/main/java

# Path to test files (optional but recommended)
sonar.tests=src/test/java

# Compiled bytecode directory (for Maven or Gradle)
sonar.java.binaries=target/classes

